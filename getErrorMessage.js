const chalk = require('chalk');
var resultG;
const getErrorMessage = (result, sample, type) => {
  let errorMessage = '';
  if (type === 'application/json') {
    result.
  }
};

console.log(typeof(resultG))

module.exports = getErrorMessage;
